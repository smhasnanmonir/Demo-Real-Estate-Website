import React from "react";
import { Link } from "react-router-dom";
import PropertyCard from "../PropertyCard/PropertyCard";

const Intregrated = () => {
  return (
    <div>
      <PropertyCard heading="We currently do not have any Integrated Place"></PropertyCard>
    </div>
  );
};

export default Intregrated;
